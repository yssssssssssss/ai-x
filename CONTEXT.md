# 用户研究任务编排

本上下文定义用研 AI 将研究需求转化为可验证交付物时使用的核心业务语言，避免把运行成功误认为研究可用。

## 规划与选择

**研究方向**：用户明确确认的本次研究主意图，用于限定可采用的执行方案类型。系统可以推荐方向，但不能以关键词推断替代用户选择。
_Avoid_: 隐式场景、关键词触发类型

**执行方案卡片**：同一研究方向下供用户比较和选择的执行取向，例如快速判断、深度研究、广度扫描或整改复测；它不是一个独立任务。
_Avoid_: 任务卡片、Skill 卡片

**推荐方案**：系统根据用户表达在当前可见执行方案中标出的默认建议，只影响初始聚焦，不代替用户选择，也不决定方案是否可见。
_Avoid_: 自动选择、隐藏其他方案

## 验收与交付

**真实能力闭环**：以真实研究输入、真实 LLM 网关和至少一个真实 Tool 产生可追溯报告，并由研究员判定该报告可用于下一步研究或决策的端到端结果。
_Avoid_: API 成功、Schema 通过、演示闭环

**研究员可用性判定**：研究员对真实能力闭环产出的报告作出的可用、需重大修改或不可用的专业结论。
_Avoid_: 自动评分、模型自评

**独立研究员评审**：由不负责本次所用 Skill 或 Tool 的用研人员进行的研究员可用性判定；能力 owner 可提供修正，不能作为唯一验收人。
_Avoid_: owner 自验、业务方替代方法评审

**可用性最低线**：研究员判定报告“可用”时必须同时满足的标准：关键竞品事实可核验、研究方案可执行、对京东的建议有观察事实或待验证假设支撑、数据缺口与风险未被隐藏。
_Avoid_: 结论绝对正确、文案完整即合格

**P0 通过批次**：同一金标场景的三次独立真实运行；每次均真实调用网关和至少一个 Tool，均经独立研究员评审，至少两次为可用，且任一次均不得发生来源伪造、敏感信息泄漏或确认闸门绕过。
_Avoid_: 单次成功、纯 Mock 回归

**P0 硬失败**：来源伪造、敏感信息泄漏或确认闸门绕过；出现即停止验收，修复后重新开始三次运行。
_Avoid_: 可接受波动、事后补充说明

**P0 质量失败**：未达到两次可用的通过率，或被判需重大修改、不可用；必须归因修复后完整重跑三次，不能挑选成功样本补数。
_Avoid_: 补跑单次、忽略失败样本

**可追溯报告**：其每项结论均能区分并反查用户输入、知识库、Tool 结果、LLM 推断或待人工确认来源的研究交付物。
_Avoid_: 无来源总结、黑盒报告

**长期审计包**：长期保存的金标运行交付与复核材料，包括最终报告、评审结论、计划、版本和模型元数据、URL、截图引用及结构化 Tool 结果；不含完整原始用户材料、原图 Base64、完整提示词或未脱敏上游错误。
_Avoid_: 全量原始留存、仅保存最终文案

**竞品事实证据**：可作为竞品现状事实的、能复查 URL 或截图时间的官方页面、App 截图、官方公告或公开第三方资料。内部知识库仅提供方法依据；LLM、虚拟用户和视觉实验室输出只可作为解释或待验证假设。
_Avoid_: 模型记忆、无来源搜索摘要、不可复查转述

**金标场景**：用于端到端验证真实能力闭环的、输入和人工评审标准均预先确定的代表性研究任务。P0 的首个金标场景为“直播场域数字人竞品研究”。
_Avoid_: 通用演示、全量能力范围

**金标真实运行**:对金标场景发起的一次全真闭环运行——规划、执行、报告生成全程调用真实 LLM 网关,检索走真实 Tool,不含任何 mock provider 或 fixture。是批次审计包的最小取样单位。
_Avoid_: mock 运行、真 Tool + mock LLM 的半真运行、演示运行

**批次审计包**:一个金标批次(三次金标真实运行)长期留存的目录,批次根含聚合判定,每次运行含裁剪后的报告、计划、执行日志、模型元数据、来源核验清单与评审表单。机器只填客观计数,批次是否通过由研究员判定。
_Avoid_: 运行时任务工作区、含原始材料/Base64/完整提示词的全量留存

**基础设施失败**:金标真实运行中与研究能力无关、可辨识的运行环境故障,如网关超时或 5xx、检索配额耗尽、网络不可达。留痕后可重试补齐,不占批次三次名额。
_Avoid_: 能力失败(报告已产出但质量不达标)、以 infra 为名重跑能力失败样本

**Tool 可用性层级**:Tool 按是否平台常在分层。`core` 为托管、常在的公开检索能力(如网页检索),是关键结论证据的必经来源,失败按基础设施失败处理;`optional` 为依赖外部后端的增强能力(如竞品截图库、各实验室),只作补充证据,其缺失不得阻断报告——规划不得将其置于关键结论的必经依赖,金标真实运行遇其失败自动跳过成缺口。
_Avoid_: 把增强能力当关键证据、因增强后端未就绪而判整轮失败

**评审表单**:研究员对单次金标真实运行做可用性判定的结构化载体。逐条勾选可用性最低线,并对报告中每条 tool_result 结论的来源引用做逐条打开核验。机器预填客观字段,判定与结论字段只能由独立研究员填写。
_Avoid_: 自由格式结论、模型代填判定、只看来源是否标注而不实际核验

## 研究任务结果

**研究规划任务**：回答“如何开展研究”的任务，交付研究目标、方法、样本、排期、采集模板和验收标准，不代表真实研究已经执行。
_Avoid_: 将研究方案标记为研究结论、用待验证问题替代业务答案

**研究回答任务**：基于当前可用的用户输入、Knowledge、Tool 与数据证据，直接回答业务研究问题，并区分已支持结论、暂定假设和未回答缺口。
_Avoid_: 只有方法建议、问题清单或“建议后续研究”的报告

**直接答案**：针对一个必答研究问题给出的结论，必须同时说明证据、置信度、业务含义、建议行动和仍需验证内容。
_Avoid_: 问题复述、分析过程、无证据的绝对结论

**请求交付物**：用户明确要求本次任务实际生成的结构化内容，如策略地图、心智模型、设计原则或机会点清单；只在计划中列出名称不算交付。
_Avoid_: deliverables 字符串清单、空章节、占位内容

**动态研究报告**：以直接答案为固定骨架、按真实研究主题动态组织章节的报告；章节可变化，但必须完整覆盖必答问题、请求交付物、证据和风险。
_Avoid_: 无约束自由文本、固定空章节、从未复核中间产物直接拼接

**开放式动态报告**：由模型决定章节标题、数量、顺序和类型化内容组合，但由系统固定答案覆盖、证据约束、请求交付物和风险披露的动态研究报告；布局异常只能降级展示，不能改写研究内容。
_Avoid_: 任意 Markdown、模型自定义 Schema、布局失败等同内容失败

**布局蓝图**：只引用已 Review 的 Canonical Deliverable 内容节点并描述章节分组和顺序的展示计划；它不能新增、改写或删除研究结论。
_Avoid_: 第二份研究报告、未经 Review 的摘要、自由内容生成

**内容失败**：必答问题、请求交付物、Evidence 或风险披露不满足交付合同，必须阻断交付。
_Avoid_: 章节标题不理想、布局模型不可用

**布局降级**：布局蓝图不可用时，系统在不改变 Canonical Deliverable 内容的前提下采用确定性顺序完成报告展示。
_Avoid_: 重新研究、静默丢弃内容、把降级误报为内容失败

**无损 Canonical 编译**：Step 10 将已 Review 的语义 Draft 转换为 Canonical Deliverable 时，只能规范化引用、生成机器元数据和追加明确缺失的请求内容；不得删除、重排或隐式改写已有语义单元。
_Avoid_: 为通过 Schema 压缩报告、第二次全文重写

**类型化内容补丁**：结构修复或语义修订使用的显式操作清单；每项操作必须指向稳定 key，结构修复只能改绑定或追加缺失内容，不能修改正文。
_Avoid_: 返回整份替换 Draft、自由 JSON Patch、无目标全文重写

**审校草稿预览**：Canonical 门禁失败时向任务所有者展示的脱敏内容摘要；它明确不是正式报告、不可导出或发布，仅用于确认已有工作未丢失。
_Avoid_: 将失败 Draft 当作 Canonical Deliverable、绕过 Evidence Gate 发布
