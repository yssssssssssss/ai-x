# 用户研究任务编排

本上下文定义用研 AI 将研究需求交给一个或多个 Skill，并把结果直接交付为研究报告时使用的核心业务语言。运行成功不等于研究可用；系统必须忠实暴露输入缺口和执行失败。

## 规划与输入

**研究方向**：用户明确确认的本次研究主意图。系统可以推荐方向，但不能以关键词推断替代用户选择。
_Avoid_: 隐式场景、关键词触发类型

**执行方案卡片**：同一研究方向下供用户比较和选择的执行取向。卡片说明会运行哪些 Skill、依赖顺序、缺口处理和取舍；它不是独立任务。
_Avoid_: Task 卡片、Skill 卡片、隐藏执行步骤

**编排模式**：用户在创建任务前选择的 `single_skill` 或 `multi_skill`。两种模式共用同一套 1..N Skill 执行内核；模式只约束方案中 Skill 数量和最终报告方式，不代表质量等级。
_Avoid_: 双执行引擎、运行时自动切换模式、高级模式

**RequirementContext**：一次任务的目标、范围、已解析输入、明确假设和信息缺口。它是规划和执行共享的最小需求上下文。
_Avoid_: 多代 Requirement Schema、从关键词隐式补全必需事实

**Skill 输入声明**：Skill 在 `SKILL.md` 中声明的输入项、是否必需、可接受来源、用户问题和缺失处理。问询只能来自已选方案中真实存在的输入声明。
_Avoid_: 前端硬编码问题、与执行无关的通用问卷

**输入绑定**：将对话、上传、权限内数据库、Knowledge 或只读 Tool 的一个真实值绑定到一个或多个 Skill 输入。相同输入只解析和询问一次。
_Avoid_: 重复问询、无归属数据库读取、模型猜测输入

**信息缺口（Gap）**：用户确认无法提供或执行失败后仍未满足的可降级输入或结果。Gap 必须进入最终报告；必需且不可降级的输入缺失时停止执行。
_Avoid_: 静默忽略、伪造默认值、用空内容冒充完成

## Skill 与方案

**SkillDefinition**：一个 Skill 的唯一运行时定义，由 `SKILL.md` 的 Frontmatter 和正文共同组成，声明用途、输入、Knowledge、Tool、执行方法、报告结构和允许的降级结果。
_Avoid_: 独立手写 Skill Registry、另一个执行合同

**SolutionPlan**：用户确认后冻结的 Skill DAG。它保存本次使用的 Skill 定义快照与 hash、输入绑定、依赖、最终报告 Skill 和失败策略。
_Avoid_: 运行中读取最新版定义、执行时新增隐藏步骤、跨模式降级

**Skill 定义快照**：计划确认时保存的 Skill ID、正文、运行声明和内容 hash。新定义只影响新任务或显式 Replan，不能改变运行中或恢复中的任务。
_Avoid_: 多版本 Reader、后台 Watcher、静默漂移

**支持 Skill**：在 Multi Skill 方案中回答限定子问题并返回 `ReportResult` 的 Skill。其结果作为最终报告 Skill 的输入 Artifact，不是第二份正式报告。
_Avoid_: Contributor 协议、子报告拼接、隐藏 Agent

**最终报告 Skill**：Multi Skill 方案中唯一负责综合全部可用 Skill 结果、来源和 Gap 的 Skill。它只执行一次综合调用，并按自身报告结构输出最终 `ReportResult`。
_Avoid_: 通用 Composer、多轮 Reviewer／Repair、临时替换报告作者

## 执行与结果

**ReportResult**：Skill 结果与正式报告共用的唯一内容合同，只包含标题、摘要、有序章节、来源、信息缺口和状态。章节内容块只有文字、列表、表格和图片。
_Avoid_: Canonical Deliverable、ReportDocument、ReportPackage、行业专用报告版本

**Single Skill 报告**：唯一 Skill 的 `ReportResult` 直接成为正式报告，不再调用第二个报告模型。
_Avoid_: 报告重写、独立摘要报告、执行后内容审校流水线

**Multi Skill 报告**：支持 Skill 结果由最终报告 Skill 做唯一一次综合后形成的 `ReportResult`。重复结论合并，冲突和失败显式保留；综合失败时才确定性分组已有结果并输出 `partial`。
_Avoid_: 多份报告首尾拼接、多个最终作者、静默裁决冲突

**结果状态**：`complete` 表示明确要求均已有结果；`partial` 表示已有可用报告但存在 Gap 或失败 Skill；`failed` 表示没有形成可用报告。
_Avoid_: 用布局或文案问题阻断内容、把 partial 标为失败

**可追溯来源**：能够解析到本次用户材料、Knowledge 或 Tool 输出的来源。未知来源 ID 在交付边界丢弃并记录 Warning；没有来源的内容不展示引用，也不被标成已验证。
_Avoid_: 模型记忆来源、无效 URL、来源等级推断

**确定性 HTML Renderer**：只把 `ReportResult` 安全转义为 Web、下载和打印共用的静态 HTML。Renderer 可以控制字体、颜色、间距、目录和打印样式，不能增加、删减或改写研究内容。
_Avoid_: LLM 生成 HTML／CSS／JavaScript、远程运行时资源、第二事实源

**可选发布出口**：Zero 等外部发布能力只消费已完成报告。发布失败不改变报告完成状态，外部写入必须获得 owner 确认。
_Avoid_: 把发布当报告门禁、自动外部写入

## 验收与质量

**真实能力闭环**：以真实研究输入、真实 LLM 网关和至少一个真实 Tool 产生可追溯报告，并由研究员判定该报告可用于下一步研究或决策的端到端结果。
_Avoid_: API 成功、Schema 通过、演示闭环

**研究员可用性判定**：研究员对真实能力闭环产出的报告作出的可用、需重大修改或不可用结论。它是发布前评估方法，不是普通任务 Runtime 门禁。
_Avoid_: 自动评分、模型自评、阻塞普通内部只读任务

**独立研究员评审**：由不负责本次所用 Skill 或 Tool 的用研人员进行的研究员可用性判定；能力 owner 可以提供修正，不能作为唯一验收人。
_Avoid_: owner 自验、业务方替代方法评审

**可用性最低线**：关键事实可核验、研究方案可执行、建议有观察事实或明确待验证假设支撑、数据缺口与风险未被隐藏。
_Avoid_: 结论绝对正确、文案完整即合格

**金标场景**：输入和人工评审标准预先确定的代表性研究任务。当前 P0 金标场景为“京东众筹频道 0→1 用户与增长策略研究”。
_Avoid_: 通用演示、临时挑选成功样本

**P0 通过批次**：同一金标场景三次独立真实运行；每次真实调用网关和至少一个 Tool，并经独立研究员评审。至少两次可用，且任一次均不得发生来源伪造、敏感信息泄漏或确认闸门绕过。
_Avoid_: 单次成功、纯 Mock 回归

**基础设施失败**：网关超时或 5xx、检索配额耗尽、网络不可达等与研究能力无关的运行环境故障。留痕后可以重试补齐，不占批次名额。
_Avoid_: 报告已产出但质量不达标、以 infra 为名重跑能力失败样本

**长期审计包**：长期保存的最终报告、评审结论、冻结 Plan、版本与模型元数据、URL、截图引用及结构化 Tool 结果；不含完整原始用户材料、原图 Base64、完整 Prompt 或未脱敏上游错误。
_Avoid_: 全量原始留存、只保存最终文案

## 安全边界

普通内部只读研究不经过通用 Legal 或 Security 审批。系统只在真实边界执行一次校验：用户／项目／任务隔离，Tool 文件与网络权限，密钥和敏感字段脱敏，请求大小与超时，取消和失败隔离，HTML 转义，以及外部写入、付费或不可逆操作的 owner 确认。

文案质量、证据强弱、视觉质量和研究员意见形成 Warning 或离线验收结论，不扩张为运行时审批系统。
