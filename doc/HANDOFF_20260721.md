# 会话交接文档 · 用研 AI 项目(2026-07-21)· 移交给下一个 AI 工具

> **给下一个 AI**:读完本文即可无缝接手。含现状、坑、待办、5 个关键决策的历史依据。
> **配合读**:`doc/会话交接文档_HANDOFF_20260720.md`(上一份,7-20 早间的架构底 + M1-C 现状)。
> **本文写作时间**:2026-07-21 · 交接方:Claude(feat/m1-real-capabilities 分支)

---

## 0. 60 秒读完:你要接手什么

**项目**:用研 AI = query 驱动的 Agent 平台,聚合各团队用研能力(22 skill)为可调用系统。
**目录**:`/Users/heyunshen/work/PROJECT/jdc/ai-x`
**分支**:`feat/m1-real-capabilities`(未 push,本会话累积 10+ commit)

**用户最新态度(极其重要)**:
- 已不满于**反复的技术层"修修跑跑"验证**。用户明确表达:"这个测试是在验证什么,只是在验证模型是否可以在当前项目中完成任务吗"
- 用户选择的方向:**先把业务盲评招募包写好**(已完成 → `doc/M1-业务盲评招募包_20260720.md`),再决定是否发出去
- KISS/YAGNI 是硬性偏好(见 memory `feedback_engineering-style`)
- 中文 terse,findings-first,严禁过度工程化

**当前尴尬状态**:
- 5 项修复已 commit,理论上 6/6 case 端到端跑通已就绪
- 但 4 轮 batch 都被外因中断(见 §5 事故时间线),**从未一次性完整跑完过 6 个用研 skill**
- 招募包已完备,可以发布但**用户没让我发**

---

## 1. 项目当前完成度(实测数据,不是估算)

| 轴 | 进度 | 证据 |
|---|---|---|
| A 轴 · 架构 | ~90% | 四段流 + 容错 + SSE + domain 预筛 + KB 索引注入 + 多模型 fallback + 编排收敛,全部落地 commit |
| B 轴 · 真实能力 | ~92% | 9/9 tool real · 22 skill 全登记 · 178 篇 KB 同源导入 |
| 端到端验证过的 skill | **5/22** | digital-human / competitive-web-research / competitive-app-analysis / design-experience-review / **generate-interview-guide** |
| 数据源真实性 | 9/9 real | 见 `scripts/capability-check.ts`,o2-web-search fake 已删 |
| **业务价值层验证** | **0/N** | 招募包 `d7010c1` 已备好但**未发**,业务专家从未试用过 |

---

## 2. 本会话累积 commit 清单(按时间倒序 · 17 个)

```
55bdf0e fix(orchestrator): selectPlan.assumptions 兜底非数组类型
d7010c1 docs: M1 业务盲评招募包(试用手册+邀请函+反馈问卷)
8af75d7 refactor(llm): 备选模型换成 GPT-5.4 主 + GPT-5 备(移除 Kimi/Gemini)
b9aea74 fix(orchestrator): planPhase 加编排收敛约束,防 LLM 堆叠
a02ee42 fix(llm): Gemini 长输出截断 + parse 错误消息健壮性
73ee4c6 feat(llm): 网关客户端支持多模型热切换 + OpenAI/Gemini 双协议
d99fd6d fix(user_research): 修 skill domain 语义 bug + 批量脚本超时
c11651e docs: M1-D+ 6 skill 批量验证评估报告
5eec19a test(scripts): 批量 skill 端到端验证脚本
e1a6c98 docs: 2C-DesignWiki 接入方案 + 用研访谈提纲评估报告
d12f38f test(scripts): 用研 skill 端到端脚本 + 2C 源差异检查
3b43b48 feat(user_research): 补 domain 预筛 + skill 分支注入 KB 索引
f6b8ffe refactor: 删除 o2-web-search(fake),真实能力路径清理
302f88e docs: M1 真实评估报告 + 交接文档;.gitignore 排除 .env.bak
15e0b79 test(scripts): 能力自检 + 端到端真跑脚本
0f78f85 feat(tools): 接入 3 个真实检索 tool + 登记接线
869ee2c feat(runtime): 执行引擎容错化 + 真实 tool 通道(O2Launch/Webcli/body_inject)
```

---

## 3. **重要 · 工作区当前有 15 处 uncommitted M 文件**(⚠️不是我改的)

**别盲 commit 这些改动**。它们来源不明,是并行会话/编辑器插件/其他工具做的:

- `apps/agent-api/src/routes/tasks.ts` `apps/agent-api/src/server.ts`
- `apps/orchestrator-runtime/src/runtime/gateway-llm-client.ts`(**+242 行**,加多模态 image 抽取 + context 治理,细节见 §5 事故时间线)
- 前端 6 个:`App.tsx / api/client.ts / stages/Stage2Plan.tsx / stages/Stage4Report.tsx / pages/Workbench.tsx / theme.css`
- `database/db.ts` `schemas/research-report.schema.json`
- `tests/execute-fault-tolerance.test.ts`
- `external-tools/vision-brand-lab/apps/server/src/services/visionBrandService.ts`
- `apps/web/package.json` + `pnpm-lock.yaml`(依赖变化)

**处理原则**:
- 不 revert(别人在做的事看着方向对,如 client 加多模态是解决交接文档 §4 "text base64 直喂 LLM 超时"老坑)
- 不 commit(不知道逻辑完整性,不清楚 tests 是否通)
- 你的第一件事应该是**问用户**:「工作区有 15 处非我改动,你希望怎么处理?」

**另有 untracked 文件**:`.env.bak-preswitch / .pids/ / apps/agent-api/src/routes/topology.ts / apps/orchestrator-runtime/src/report-renderer.ts / apps/web/dist/ / apps/web/src/components/FlowChart.tsx / apps/web/src/pages/FlowChartDemo.tsx / dev.sh / doc/report-demos/ / external-tools/users-research-all/` 等,均不是本会话产生。

---

## 4. 5 项关键修复(理论上联合可让 6/6 全通,未联合验证)

| commit | 修的问题 | 关键细节 |
|---|---|---|
| `d99fd6d` | **domain 语义 bug** | `[user_research, cross_cutting]` 里 user_research 是死字符,cross_cutting 兜底掩盖。改成 `[<真实 task_type>, cross_cutting]`。22/22 覆盖。见 memory `feedback_domain-must-be-task-type` |
| `73ee4c6` + `8af75d7` | **网关配额耗尽 + 多模型 fallback** | GPT-5.2 配额耗尽(429 code:2001)。加 `LLM_MODEL_FALLBACKS` 环境变量。当前主 GPT-5.4 备 GPT-5(**Kimi/Gemini 已移除**,原因见 §5 Run 3) |
| `b9aea74` | **planPhase LLM 编排失控 13 步** | Run 2 case 1 大合唱 20 分钟未跑完。prompt 加 `depth ≤8 / speed ≤4 / 拒绝堆叠` 硬约束 |
| `55bdf0e` | **selectPlan.assumptions 兜底 bug** | Run 3 case 2 崩 `(cand.assumptions ?? []).map is not a function`。改成三段防御:数组 → 直接用,对象 → Object.entries 转,字符串 → 包成数组,其他 → 空 |

---

## 5. **事故时间线** · 4 轮压测都被外因中断(必读)

### Run 1(2026-07-19 · 旧 domain, GPT-5.2 主)
- 5/6 skill 命中 · 4 跑通 · 1 超时(journey-map ~1200s+)
- 暴露:domain 语义 bug + 轮询超时不够

### Run 2(2026-07-20 早 · 新 domain, GPT-5.2 主)
- 1/6 完整 · case 1 编排 13 步大合唱超时 · **GPT-5.2 配额耗尽**,case 2-6 全 429
- 暴露:LLM 编排失控 + 网关额度限制

### Run 3(2026-07-20 晚 · 新 domain + Kimi 主 + Gemini 备 + 编排收敛)
- 0/3 完成(用户中止)· case 1 Kimi 复杂 JSON 崩 · case 2 selectPlan.assumptions 崩 · case 3 execute 15 分钟未完
- 暴露:**Kimi 简单 JSON 稳但嵌套 JSON 不稳**、selectPlan bug、单步执行慢

### Run 4(2026-07-20/21 凌晨 · 5 项修复齐 + GPT-5.4 主)· **决定性失败**
- 6/6 全 `fetch failed`
- **根因**:batch 运行的 30 分钟内,**并行会话在改 `gateway-llm-client.ts` 9 次**(每次 tsx watch 触发 agent-api 热重启,断我 batch 的正在进行请求)
- 结果:6 个 case **没有一个到达 orchestrator 层**(taskId 都是空)
- **这不是逻辑 bug,是环境撞车**

### 并行会话对 client 的改动(未验证但方向合理)
+242 行,加了:
- `extractImagesFromContext` — 提取 image dataUrl 变 `[image_ref:N]`,避 base64 膨胀 context(**解决交接文档 §4 老坑**)
- `trimContextIfNeeded` — tool_outputs 滑动摘要,超预算保留最近 2 条
- `MODEL_CONTEXT_LIMIT`(默认 262144)/ `CHARS_PER_TOKEN`(3)/ `KEEP_RECENT_OUTPUTS`(2)常量
- OpenAI/Gemini 多模态 content 构造(text + image_url parts / inlineData)

---

## 6. **13 个踩过的坑**(严禁再踩)

1. **o2-web-search 是 fake data**(已 `f6b8ffe` 删除)
2. **domain 死字符坑**:`[user_research]` 靠 cross_cutting 兜底掩盖 bug,orchestrator 段2c 按 task_type 精确匹配,user_research 永远不命中
3. **GPT-5.2-joybuilder 配额耗尽**,error.code=2001,窗口内不恢复,别用它做主
4. **Kimi 输出结构**:`content` 常空,内容在 `message.reasoning_content`。parseOpenAI 已加 fallback
5. **Kimi 复杂嵌套 JSON 不稳**(planPhase execution-plan-candidates 崩过)。这是本会话弃 Kimi 的直接原因
6. **planPhase 若不加步数约束,LLM 会 13 步大合唱**——把可选 skill 全塞进去(`b9aea74` 已修)
7. **selectPlan.assumptions**:LLM 会返对象 `{a1:..., a2:...}` 或字符串,不只是数组(`55bdf0e` 已修)
8. **tsx watch 撞车**(本会话真实事故):并行改 `apps/orchestrator-runtime/*` 会热重启 agent-api,断掉正在跑的 batch。**开跑 batch 前必须确保没别人在改 apps 目录**
9. **fetch 5 分钟 headers timeout**:12+ 步执行 >5min,前台 fetch 会断。测试脚本轮询磁盘 `report.json` 而不是 await execute
10. **tsx -e 顶层 await 不支持**:CLI 走 CJS 模式。脚本必须放 `scripts/`(那里有正确 tsconfig)
11. **text base64 直喂 LLM 会超时**:22KB SKILL.md + base64 资料 → gateway timeout。已在 runStep 解码 text/* 为明文
12. **.env 改动 tsx watch 不自动生效**:tsx watch 只热载代码。改 .env 必须 `pkill -f "tsx watch apps/agent-api"; nohup npm run api:dev > /tmp/api.log 2>&1 &`
13. **skill-registry.yaml 447 行超读取预算**:用 awk/sed/python 精确定位段落,别整读

---

## 7. 关键文件地图(与上次 HANDOFF 相同 + 新增)

### 与上次相同(仍是权威)
| 文件 | 作用 |
|---|---|
| `apps/orchestrator-runtime/src/orchestrator.ts` | 四段流核心(planPhase/selectPlan/executePhase/runStep/finalizeReport) |
| `apps/orchestrator-runtime/src/runtime/tool-adapter.ts` | FakeO2/HttpApi/RestJson/O2Launch/Webcli/ToolRouter |
| `apps/orchestrator-runtime/src/runtime/agent-runtime.ts` | buildToolAdapter 注册各 adapter |
| `apps/orchestrator-runtime/src/runtime/config-loader.ts` | SkillRegistryEntry / ToolManifest 类型 |
| `apps/orchestrator-runtime/src/runtime/skill-loader.ts` | listActiveSkills/Tools/loadSkillBody |
| `apps/orchestrator-runtime/src/runtime/gateway-llm-client.ts` | LLM 网关客户端(**并行会话改动大**,见 §5) |
| `orchestrator/tool-registry.yaml` | tool 索引卡,独立于 skill-registry |
| `orchestrator/skill-registry.yaml` | skill 索引卡,22 skill 全登记 |
| `tools/<id>/manifest.yaml + in/out.schema.json` | tool 定义 |
| `knowledge-base/{methods,models,assets,skills}/` | 178 篇 KB,已从 2C-DesignWiki 同源导入 |
| `apps/agent-api/src/routes/tasks.ts` | /plan(SSE) /select /execute /resume |
| `apps/web/src/pages/Workbench.tsx` | 前端主状态机 |

### 本会话新加
| 文件 | 作用 |
|---|---|
| `apps/orchestrator-runtime/src/orchestrator.ts:21-38` | `loadResearchWikiIndex()` — user_research 域 skill 运行时读到的 KB 导航 |
| `apps/orchestrator-runtime/src/orchestrator.ts:451-459` | selectPlan.assumptions 三段兜底 |
| `tools/joyspace-search/`(NEW) | webcli/joyspace,京东内部知识检索,复用浏览器 SSO |
| `scripts/test-skills-batch.ts` | 6 skill 批量端到端脚本 |
| `scripts/test-llm-switch.ts` `test-llm-two-models.ts` `debug-kimi-json.ts` | LLM 网关多模型验证脚本 |
| `scripts/sync-2c-kb.mjs` | KB 与 2C 源差异检查(diff-only,不覆盖) |
| `doc/2C-DesignWiki_接入方案_20260720.md` | 接入方案文档 |
| `doc/端到端真实评估报告_用研访谈提纲_20260720.md` | 首验报告 |
| `doc/端到端真实评估报告_6skill批量_20260720.md` | Run 1 汇总 |
| `doc/M1-业务盲评招募包_20260720.md` | ⭐ **业务盲评招募包(试用手册+邀请函+反馈问卷)** |

### skill 实体位置(易踩坑,牢记)
- **KB 派生 skill 22 个**都在 `knowledge-base/skills/<id>/SKILL.md`(2C-DesignWiki 同源导入)
- **原生 4 个 skill** 在 `skills/competitive-analysis/{digital-human,web-research,app-analysis}/` + `skills/design-review/`
- 全部 22 个 skill 在 `orchestrator/skill-registry.yaml` 登记,`path`/`entry` 字段指向具体位置

---

## 8. 环境与服务状态

- **agent-api**:tsx watch 端口 3001,当前**活着**(healthz OK)
- **前端**:portal-web,vite 端口 5173/5174(可能空闲)
- **5 lab 服务**:AESTHETIC/ATTENTION/EXPERIENCE/VIRTUAL_USER/VISION_BRAND(端口见 .env,均活着)
- **LLM**:`LLM_PROVIDER=gateway`(真实内网),`LLM_MODEL_NAME=GPT-5.4-joybuilder`,`LLM_MODEL_FALLBACKS=GPT-5-joybuilder`,`LLM_GATEWAY_TIMEOUT_MS=240000`
- **测试账号**:email `tester+ui@local.dev` password `testtest`(前端 localStorage 有 token)
- **o2 CLI**:metasearch 通、o2-crawler 需 CRAWL_TOKEN、oxygen-comment/sku-selection/o2-ge-search 缺数据菜单权限(待用户申请)

**重启命令**:
```bash
cd /Users/heyunshen/work/PROJECT/jdc/ai-x
pkill -f "tsx watch apps/agent-api"
sleep 2
nohup npm run api:dev > /tmp/api.log 2>&1 &
# 等 healthz 通
for i in 1 2 3 4 5 6 7 8; do sleep 2; curl -sf http://localhost:3001/api/healthz && break; done
```

**复现真实报告**:
```bash
npx tsx scripts/capability-check.ts       # 应 9/9 real
npx tsx scripts/test-llm-two-models.ts    # 应 GPT-5.4/5 各 3/3 pass
npx tsx scripts/test-skills-batch.ts      # ⚠️ 跑前确认没别人在改 apps/(见 §5 Run 4 事故)
```

---

## 9. 待办清单(按优先级)

### 🔴 P0 · 用户第一件要问的事
- **工作区 15 处 uncommitted 改动怎么处理**(见 §3)
- 用户需要给方向:让作者自 commit / 一并整理入库 / revert 走

### 🟡 P1 · 关键决策(用户已选过,持续中)
- **业务盲评推进** —— 招募包 `doc/M1-业务盲评招募包_20260720.md` 已备好。用户当前状态:选了 C(先备文档)不选 B(直接发),再问推进方式
- **不要再启第 5 轮技术层 batch**,除非用户明确要求(边际递减)

### 🟢 P2 · 环境层待办
- 京东内部数据菜单权限申请(待用户去黄金眼/采销工作台申请,不是代码问题):
  - oxygen-sku-selection · 黄金眼「搜索词分析菜单」权限
  - o2-ge-search · 黄金眼「专用菜单」权限
  - oxygen-comment · 采销工作台商品评价权限
- ai-spider-search 服务(localhost:8000)未起,capability-check 会 fail

### 🔵 P3 · 未来价值验证
- 挑 3-5 个真实用研/设计/竞品同事,发招募包收反馈
- 达标线:可采纳率 ≥ 70% + 溯源均值 ≥ 4/5 + 时间收益 ≥ 3h/周 + 推荐率 ≥ 50%
- 达标任意 2 项 → 值得继续投入;全达标 → 进 M2

---

## 10. 关键架构决策(不要改,除非用户明确要求)

1. **domain 必须是 task_type 值**:orchestrator 段2c 按 task_type 精确预筛(见 [orchestrator.ts:267-273](apps/orchestrator-runtime/src/orchestrator.ts:267))。写主题分类(如 user_research)是无效死字符
2. **KB 与 2C 源同步**:**diff-only,不 rsync**。项目 KB 是 2C 派生+增强版(indexer 加了 21 行 frontmatter),盲 rsync 覆盖会 -3715 行倒退。已改脚本 `scripts/sync-2c-kb.mjs` 为只读
3. **LLM 网关配额分模型独立**:GPT-5.2 耗尽不影响 GPT-5.4/5。fallback 机制见 `apps/orchestrator-runtime/src/runtime/gateway-llm-client.ts`
4. **planPhase 编排必须收敛**:硬约束 depth ≤8 / speed ≤4 / 拒绝堆叠。否则 LLM 会大合唱把 gateway token 耗光
5. **user_research 域 skill 运行时注入 KB 索引**:`orchestrator.ts:21-38 loadResearchWikiIndex()` — 只喂 4 个叶子 index(~23KB),不喂 129 篇正文(会 timeout)
6. **红线**:严禁 `if task_type == 'xxx'` 硬编场景分支(见 memory `feedback_engineering-style`)

---

## 11. 用户风格提示(与之协作必读)

**记忆链**(见 `/Users/heyunshen/.claude/projects/-Users-heyunshen-work-PROJECT-jdc-ai-x/memory/MEMORY.md`):

- 中文 terse,findings-first,不废话
- 严禁过度工程化,KISS/YAGNI 是硬性偏好
- 方案文档统一放 `doc/`,一般更新已有的 `doc/用研AI专项_底座搭建方案_优化版_claude.md`,不另起新文件(见 `feedback_canonical-plan-doc-location`)
- 项目 KB 里的 SKILL.md 有 indexer 增补 frontmatter,是**派生+增强版**,不是 2C 原版(见 `reference-wiki-has-rich-frontmatter`)
- 用户 ERP:heyunshen · GPT-5.2 配额已耗尽(见 `project_o2-business-cli-auth-vs-permission` 和 `project_llm-gateway-multimodel-fallback`)

**用户上次话原文**(2026-07-20 深夜):

> "这个测试是在验证什么,只是在验证模型是否可以在当前项目中完成任务吗"

——用户在追问**验证的意义**,不再想看"又跑一轮 batch 又修一个 bug"。你如果接手,注意别陷入这个循环。

---

## 12. 立即上手指令模板

```
【项目根】/Users/heyunshen/work/PROJECT/jdc/ai-x
【分支】feat/m1-real-capabilities
【立即读】
  1. doc/HANDOFF_20260721.md ← 你手里这份
  2. doc/会话交接文档_HANDOFF_20260720.md(架构层)
  3. doc/M1-业务盲评招募包_20260720.md(产品价值层核心)
【立即问用户】
  1. 工作区 15 处 uncommitted 改动怎么处理?
  2. 招募包要不要发?先发几个人?
  3. 是否需要跑第 5 轮技术验证?(建议不跑)
【不要做】
  - 不擅自 commit 别人的改动
  - 不擅自 revert
  - 不重启 batch 除非用户明确要求
  - 不硬编场景分支(红线)
```

---

_v1.0 · 由 Claude Opus 4.8(1M context)编写于 2026-07-21 · 移交给下一个 AI 工具_
