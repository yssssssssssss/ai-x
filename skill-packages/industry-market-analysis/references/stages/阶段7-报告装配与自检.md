# 阶段 7 · 报告装配与自检

> 前 6 阶段的产出散在多个文档,本阶段把它们装成一份最终报告,并跑自检清单确保交付质量。

## 目标

- 复制对应档位的模板到品类目录
- 把前 6 阶段的产出**按 10 维度归位**填充模板
- 补齐 H(可衡量指标)、I(方法论)两个"临门一脚"章节
- 走完 22 项自检,确保没漏
- 产出 1 份 `.md`(内容底稿)→ 渲染成 1 份 **HTML 汇报页**(最终交付物)

> **交付形态**:`.md` 是内容底稿(改起来快、便于自检),**最终交付是一份自包含的 HTML 汇报页**——4 Tab 分类(行业洞察 / 竞品分析 / 体验诊断 / 设计策略)+ 固定头部 + Tab 内侧边栏,浏览器打开即可汇报。规范见本阶段末尾「HTML 汇报页输出规范」。**先用 .md 跑完内容、过完自检,再渲染 HTML**——内容与形式解耦,返工最少。

## 前置输入

- 阶段 1-6 的所有中间产出物
- `模板/报告模板-{档位}.md`

## 具体动作

### Step 1 · 复制模板到品类目录

```bash
cp [LOCAL_HOME]/Desktop/行业市场分析工作流/模板/报告模板-中档.md \
   [LOCAL_HOME]/Desktop/{品类}行业分析/{品类}行业市场分析.md
```

命名规范:`{品类}行业市场分析.md`(不要用"体验分析"或"竞品调研"等歧义命名)。

### Step 2 · 填入 10 维度产出

按下表把前 6 阶段的产出填入对应章节:

| 报告章节 | 素材来源 | 来自阶段 |
|---|---|---|
| 摘要 & TL;DR | 手动写(最后) | 阶段 7 |
| 一、体验问题(E) | 京东内诊报告 | 阶段 3 |
| 二、行业趋势(A) | 行业报告/数据摘录.md | 阶段 2 |
| 三、用户洞察(B) | 用户分群/决策链路 | 阶段 3(部分)+ 阶段 2 |
| 四、供给侧分析(C) | 内容阵地/KOL清单.md | 阶段 2 |
| 五、竞品与参考(D) | 竞品分析 6 板块(选样/四要素总览/商业对比/体验对比/心智象限/采集清单),套 `模板/竞品分析-6板块.md` | 阶段 2 |
| 六、商业与经营逻辑(J) | 商业/售卖/选品逻辑 + 设计含义 | 阶段 2(外部)+ 阶段 3(京东内部) |
| 七、机会与策略(F) | 机会打分表 + 优先级 | 阶段 5 |
| 八、设计语言推导(G) | 推导链 + 色板 + 组件 | 阶段 6 |
| 九、KPI 假设(H) | 手动写(本阶段补齐) | 阶段 7 |
| 附录 I(方法论) | 手动写(本阶段补齐) | 阶段 7 |
| **升档指引(文末固定块)** | **当前档给到什么 + 什么信号该升 + 升上去补哪几项(增量不重跑);模板已内置,按品类填信号** | 阶段 7 |

> **升档指引是每份报告的固定收尾块**,不可省。目的:让拿到报告的人知道"这份能用到什么程度、想更深该补什么"——补数是增量追加,不重跑整份。四个报告模板文末都已内置该块,重档为"已最高档 + 可补强方向"。

### Step 3 · 补齐 H 层(KPI 假设)

按下面模板填(轻档跳过、中档主要机会补、重档全补):

```markdown
## 八、KPI 假设

### 8.1 成功指标

| 机会 | 主指标 | 副指标 | 基线 | 目标假设 | 时限 |
|---|---|---|---|---|---|
| 榜单信息重构 | 榜单页 CTR | 榜单页停留时长 | X% | +Y% | 上线后 4 周 |
| ... | ... | ... | ... | ... | ... |

### 8.2 ROI 预判

- 投入:设计 X 人日 + 前端 Y 人日 + 产品 Z 人日
- 预期收益:{基于指标目标,估算 GMV / UV / 转化增量}
- 收益/投入比:{}

### 8.3 前置假设

改动要生效,以下假设必须成立:
- 假设 1:用户对榜单页的诉求是"快速找到值得买的"(可通过用户访谈验证)
- 假设 2:目前榜单页 CTR 偏低的原因是"信息密度过高"(可通过热力图验证)
- 假设 3:{...}

### 8.4 风险与回退

| 风险 | 等级 | 回退方案 |
|---|---|---|
| 榜单点击率不升反降 | 中 | 保留旧版入口,A/B 灰度 |
| ... | ... | ... |
```

### Step 4 · 补齐 I 层(方法论与附录)

```markdown
## 附录 I · 方法论与附录

### I.1 研究方法

本报告采用以下方法:
- 桌面研究(行业报告二手数据)
- 竞品启发式评估(N=X 家竞品,各截取 X 个关键页)
- 京东内部数据(如可获得)
- {中/重档补:一手用户访谈 N=X 人,时长 X 分钟/人}

### I.2 数据来源清单

**行业数据源**:
- 艾瑞咨询《XX 年 XX 行业报告》,发布日期 YYYY-MM
- QuestMobile《XX 年 XX 用户洞察》,发布日期 YYYY-MM
- ...

**竞品截图时间**:
- 淘宝频道 XX,截图日期 YYYY-MM-DD
- ...

**内部数据**:
- (如涉及)京东 XX 平台 GMV 数据,数据周期 YYYY-MM

### I.3 已知局限

- 局限 1:未做一手用户访谈,B 层洞察为定性推导(如为中档报告)
- 局限 2:XX 数据源未能双源交叉验证,标注为"估算·待核实"(共 X 条)
- 局限 3:海外竞品仅覆盖 2 家,X 家垂直平台未覆盖
```

### Step 5 · 写摘要 & TL;DR(最后写)

摘要放报告最前面,是决策者唯一保证读的部分:

```markdown
# {品类}行业市场分析 · {档位}

**版本**:v1.0 · YYYY-MM-DD
**执行人**:XXX
**档位**:中档
**主聚焦**:体验诊断

## TL;DR

- **一句话结论**:"{品类}频道当前的核心问题不是 XX,而是 XX。"
- **3 大发现**:
  1. {发现 1,含关键数字}
  2. {发现 2,含关键数字}
  3. {发现 3,含关键数字}
- **推荐 3 大动作**(短期):
  1. {动作 1}
  2. {动作 2}
  3. {动作 3}
- **成功指标**:{主指标目标}
```

TL;DR 的写作原则:
- 500 字以内
- 有 1 个"一句话结论"
- 3 大发现要有数字
- 3 大动作要具体、可执行
- 主指标要能落到数字上

### Step 6 · 每章补一句话结论

在每大章末尾用引号包围的一句话浓缩:

> "图书频道当前需要做的不是加入内容,而是让决策链路不被打断。"

好的一句话结论有 3 个特征:
- 短(不超过 30 字)
- 有对比("不是 X,而是 Y")
- 可直接被汇报引用

---

## 22 项自检清单

装完报告,逐项过一遍:

### 结构完整性(6 项)

- [ ] 每个必要章节都存在(按档位对照 `参考资料/三档深度对照表.md`)
- [ ] 每章有小标题、有段落层级
- [ ] 每章末尾有一句话结论
- [ ] 有 TL;DR,不超过 500 字
- [ ] 报告顶部有档位声明
- [ ] **报告文末有「升档指引」块(当前档给到什么 + 什么信号该升 + 补哪几项;重档为"已最高档 + 补强方向")**

### 数据可靠(4 项)

- [ ] 每个 % / ¥ / 数字都有出处,或标"估算·待核实"
- [ ] 中/重档头条数据 ≥2 独立源
- [ ] "估算·待核实"数量 < 总量的 20%
- [ ] 附录 I.2 数据来源清单完整

### 洞察深度(6 项)

- [ ] 竞品走了 6 板块流程(至少 ①②⑤:选样+四要素总览+心智象限)
- [ ] 有二维象限图,"我方目标位"落在相对空白象限(不与竞品重叠)
- [ ] 中/重档补 ③商业对比 ④体验对比;重档每格带证据编号回指 ⑥采集清单
- [ ] 机会清单有打分(用户价值/差异化/落地难度/KPI 支撑)
- [ ] 有短/中/长三档优先级,每档至少 2 条
- [ ] 主聚焦维度有 3 层展开(现象 → 原因 → 建议)

### 设计语言推导(3 项)

- [ ] 有属性→关键词→方向的推导链
- [ ] 主推方向的色板包含基础/内容/强调/中性四层,有 hex 值
- [ ] 有至少 1 张视觉方向对比表

### 决策可用(3 项)

- [ ] 中/重档有 KPI 假设表
- [ ] KPI 有基线数字和目标数字(可以是假设值,但要明标)
- [ ] 有前置假设和风险清单

---

## HTML 汇报页输出规范

> `.md` 自检通过后,渲染成一份**自包含的 HTML 汇报页**作为最终交付。设计目标:决策层不点任何 Tab 就看到结论,想深挖再按"背景→问题→方案"的顺序逐 Tab 展开。

### 核心区分:什么固化、什么按品类自定义

这套规范的关键是**骨架统一、皮肤自由**——每个品类的报告结构和阅读体验一致(可预期、好维护),但视觉调性各按品类心智生长(有辨识度、不千篇一律)。

**✅ 必须固化(所有品类的报告都一样):**

| 固化项 | 规则 |
|---|---|
| **页面结构** | 固定头部(含 TL;DR)+ 4 Tab(行业洞察/竞品分析/体验诊断/设计策略)+ 每个 Tab 内按章节维度做锚点定位。**正文占满核心宽度,章节侧边栏浮在核心区左外侧留白**。见下方结构图。 |
| **核心内容宽度** | 头部、Tab、正文核心区统一同一 `max-width`(约 1016px)、左右对齐。**正文独占这个宽度,不再与侧边栏分栏**——章节侧边栏移到核心区**左外侧的留白区**(sticky 跟随滚动);视口不够宽(约 <1240px)时,侧边栏收回核心区顶部横向排列。 |
| **4 Tab 章节归类** | A/B/C/J→行业洞察,D→竞品分析,E→体验诊断,F/G/H→设计策略(见下方映射表),不随品类变。 |
| **头部层级** | 头部各模块(标题 / TL;DR / Tab 栏)用**间距 + 细分隔线**区分层级,**不用卡片式边框+阴影+圆角**——保持轻、聚焦内容,不让"外壳"抢戏。 |
| **Tab 交互与视觉** | 选中态用**底部下划线**(文字变深加粗 + 2px 下划线 + 序号点缀),**不用重色块/深底胶囊填充**;Tab 底线**限定在核心宽度内,不贯通整屏**;**切换 Tab 保持当前滚动位置,不回滚到顶部**。 |
| **哲学呈现方式** | 头部哲学标识 + 诊断挂原则标签 + 定位标价值观,贯穿方式固定。 |

**🎨 不固化(按品类心智自定义,来自阶段6 推导):**

| 自定义项 | 由什么决定 |
|---|---|
| **字体** | 品类特点。书香→宋体 serif;科技→几何无衬线;母婴→圆润字体 |
| **配色** | 品类心智 + 阶段6 色板(继承平台中性色 + 品类主题色) |
| **图案/母题** | 品类心智。书→书页光晕;宠物→爪印;数码→几何线条 |

> 一句话:**换品类时,结构和宽度不动,只换字体、配色、图案**。这样既保证"一眼是同一套工作流产出的报告",又让每个品类有自己的气质。

### 结构:固定头部 + 4 Tab + Tab 内锚点(正文占满核心宽,侧边栏在左外侧)

```
              ┌──────────────────────────────────────────────┐
              │  报告头部(标题 / TL;DR,用间距+细线分层,无卡片)│
              │  · {品类}行业市场分析 · {档位} · v1.0 · 日期    │
              │  · 「基于京东设计哲学 v16.0」标识              │
              │  · TL;DR:一句话结论 + 3 发现 + 3 动作 + 主指标 │
              ├──────────────────────────────────────────────┤
              │ 行业洞察  竞品分析  体验诊断  设计策略  ← Tab   │
              │ ‾‾‾‾‾‾‾            (选中态:下划线,底线不贯通)│
   ┌────────┐ ├──────────────────────────────────────────────┤
   │ 侧边栏  │ │                                              │
   │(章节锚 │ │   正文区:占满核心宽度,当前章节内容 + 图表    │
   │ 点,浮在 │ │                                              │
   │ 核心区  │ │                                              │
   │ 左外侧) │ │                                              │
   └────────┘ └──────────────────────────────────────────────┘
   ← 留白区              ←──────── 核心宽度 ~1016px ────────→
```

> 侧边栏用 `position:sticky` 浮在核心区左外侧的留白里,跟随滚动;视口 <1240px 容不下时,自动收回核心区顶部横向排列。切 Tab 时侧边栏内容跟着换、**页面不回滚**。

### 4 Tab 的章节归类(固定映射,10 维度各归其位)

> Tab 命名对齐本工作流四维定位:**垂类行业洞察 / 竞品分析 / 体验诊断 / 设计策略**。竞品(D)从"行业洞察"里独立成 Tab——竞品分析是设计师高频单独查阅的一块,且本工作流已把它升级为 6 板块完整流程,值得占一个一级 Tab。

| Tab | 装的报告章节(维度) | 叙事角色 |
|---|---|---|
| **① 行业洞察** | 行业趋势(A)· 用户洞察(B)· 供给侧(C)· 商业与经营逻辑(J) | 背景:这个行业怎么运转、怎么赚钱 |
| **② 竞品分析** | 竞品与参考(D)——6 板块(选样/四要素总览/商业对比/体验对比/心智象限/采集清单) | 对标:别人占了什么位、空白在哪 |
| **③ 体验诊断** | 京东内部诊断(E)——四类问题(每条挂钩 6 原则)· 现状截图 · 舆情 | 问题:京东频道卡在哪、违反了哪条平台原则 |
| **④ 设计策略** | 机会与策略(F)· 设计语言推导(G)· KPI 假设(H) | 方案:该做什么、长什么样、投入产出 |

> **Tab 顺序即汇报叙事线**:行业洞察(背景)→ 竞品分析(对标)→ 体验诊断(问题)→ 设计策略(方案)。
> **档位无关**:轻/中/重档章节数不同,但都落进这四类,同一套 HTML 模板通吃。方法论附录(I)放页脚或「设计策略」Tab 末尾折叠区,不单独占 Tab。

### 侧边栏规则

- 顶部横向 Tab 切大类;左侧竖向锚点跳章节。
- **侧边栏浮在核心区的左外侧留白区**(`position:sticky` 跟随滚动),不挤占正文——正文独占核心宽度。视口 <1240px 容不下时,侧边栏收回核心区顶部横向排列。
- 侧边栏**只显示当前 Tab 下的章节**,切 Tab 时侧边栏内容跟着换、**页面不回滚到顶部**(保持当前滚动位置)。
- 每个 Tab 下的章节都要能被侧边栏锚点单独跳转——**多小节的 Tab 要拆成多个带 id 的 `section`**,不要把几块塞进一个 section(否则侧边栏只有一个锚点)。例:竞品分析 Tab 拆成 选样分组 / 四要素 / 心智象限 三个 section。
- 「行业洞察」装 A/B/C/J 四章;「竞品分析」列 6 板块(轻档为选样/四要素/心智象限);「体验诊断」列四类问题;「设计策略」列机会/设计/KPI。

### 平台设计哲学怎么呈现(不单独占 Tab)

哲学是"标尺"不是"章节",作为**贯穿的视觉线索**:
- **头部**:一个「基于京东设计哲学 v16.0」小标识,可点开查看三层结构(智性设计 / 4 价值观 / 6 原则)。
- **体验诊断 Tab**:每条问题标注"违反【X 原则】"的标签(颜色/图标区分 6 原则)。
- **设计策略 Tab**:差异化定位标注"锚定【X 价值观】";设计推导标注"继承/增量"。

### 图表可视化建议(能图则图,不堆表格)

> 报告默认是"表格 + 数字卡"。想让关键数据从表格里站出来,用 **lieflat-chart**(单色数据可视化 skill,已用户级安装于 `~/.claude/skills/lieflat-chart`)出图,嵌进对应章节。它和本报告审美同源(白底/克制/编辑感),融合度高。**没装或想快速出图,退回本表右列的"纯 CSS/SVG 兜底形式"也可以。**

| 内容 | 数据形状 | lieflat 推荐图型 | 纯 CSS/SVG 兜底 |
|---|---|---|---|
| 机会四维打分(F) | 少类目排名(≤8) | **F5 Tick Rows**(横向 tick,1 tick=1 分) | 分组条形 |
| 金选好评率 / 单一占比(E) | 一个百分比 | **L14 Hundred Field**(百点网格,1 点=1%) | 数字卡 |
| 四类问题 / 100% 构成(E/F) | 占比分段 | **F4 Tick Donut**(环形表盘,1 tick=1%) | 占比环图 |
| 市场规模/增速(A) | 时间序列 | **F2 Hairline Line / F3 Hairline Area** | 折线/柱状 |
| 竞品心智定位(D) | 二维象限 | (报告已有 `.quad` 象限,够用) | 二维象限图 |
| 商业逻辑→设计含义(J) | 左右对照 | (用表格) | 左右对照 |
| 三档优先级(F) | 分期泳道 | (用报告已有 `.timeline`) | 时间线泳道 |

#### 用 lieflat 出图的 4 步(实战验证过)

1. **调用 skill**:`/lieflat-chart`(或直接说"用 lieflat-chart 把这组数据画成图")。它会按"数据形状→选图型"的硬约束走,不用你指定图型名。
2. **⚠️ 只用纯 SVG 图型(Lupi Editorial / Lupi Basics),避开 Glance**。原因:Glance 和交互大图依赖 ECharts/Chart.js 的 **CDN**,一旦用了,报告就**违反"自包含单文件、离线可开"**(见本阶段技术约束 + 坑 7)。选型时明确告诉它"要纯 SVG、离线可用"。
3. **让它产"嵌入版"而非"独立单文件"**:图的墨色/边框/标签复用报告自己的 CSS 变量(`--ink` / `--border` / `--panel`),SVG 片段 + 渲染 JS 用 IIFE 包裹内联进报告,和报告已有脚本零命名冲突。
4. **保留原表格,图作增强**:在表格**下方**加 `<figure>`,不删表。断网/打印/读屏场景下数据仍在表里。

#### 两个必须处理的技术细节(踩过坑)

- **Tab 内的图要延迟渲染**:图在非默认 Tab 里时 `display:none`,SVG 没尺寸、IntersectionObserver 不触发 → 改成**切到该 Tab 才渲染**(只渲一次,用 `dataset.done` 去重),并加 `window.addEventListener('beforeprint', …)` 钩子,打印/导 PDF 前强制画全部图。
- **入场动画 + 打印**:lieflat 图默认淡入动画;务必内置 `@media (prefers-reduced-motion:reduce){animation:none}`,否则打印/截图那一刻图会偏淡甚至空白。

> 参考实战成品:`品类测试/京东宠物行业分析/图表输出/`(三种图型的嵌入版)+ 已把三图嵌入 `京东宠物行业洞察.html`(机会打分 tick / 金选百点 / 四类问题环形)。
> 图表配色遵循平台设计系统 token;lieflat 本身是单色(纸灰+炭黑),天然符合【精巧克制】。若手写兜底图,用中性色 + 单一强调色,别自造花哨配色。

### 技术约束

- **自包含单文件**:CSS 内联、图表用轻量内联 SVG 或纯 CSS 实现,不依赖外网 CDN——双击即开,可离线汇报、可直接发给他人。
- 截图用相对路径引用品类目录内的图,或转 base64 内嵌(要完全可移植时)。
- 响应式:优先保证笔记本投屏(1280px+)清晰;移动端可读即可。

### HTML 汇报页自检(渲染后过一遍)

- [ ] 头部固定,TL;DR 不点 Tab 就能看到
- [ ] **头部各模块用间距+细线分层,不用卡片式边框+阴影+圆角**
- [ ] **正文占满核心宽度(约 1016px,头部/Tab/正文同一基准);章节侧边栏浮在核心区左外侧,窄屏收回顶部**
- [ ] 4 Tab 切换正常,章节都归对了类(A/B/C/J→洞察,D→竞品分析,E→诊断,F/G/H→策略)
- [ ] **切 Tab 保持当前滚动位置、不回滚顶部;Tab 选中态是下划线(非重色块),底线不贯通整屏**
- [ ] 每个 Tab 内按章节维度做锚点定位(多小节的 Tab 拆成多个带 id 的 section)
- [ ] **字体/配色/图案由品类心智(阶段6)决定,不是套通用模板**
- [ ] 体验诊断每条问题带"违反【X 原则】"标签
- [ ] 设计策略定位带"锚定【X 价值观】"标注
- [ ] 单文件可离线打开,不依赖外网资源
- [ ] 关键数据做了图表,不是满屏表格
- [ ] **若用了 lieflat 出图:只用纯 SVG 图型(非 Glance/CDN),Tab 内图切换后能渲染,打印前 `beforeprint` 强制画全,动画有 `reduced-motion` 降级**

## 三档差异

| 项 | 轻档 | 中档 | 重档 |
|---|---|---|---|
| 自检项通过 | 至少通过 12 项 | 至少通过 17 项 | 22 项全过 |
| 摘要长度 | 300 字内 | 500 字内 | 800 字内(含研究窗口) |
| 附录 | 简版数据源 | 完整数据源 | 完整方法论+数据源+局限 |

## 常见坑

- **坑 1**:TL;DR 变成"目录导读" → 应是"结论摘要"。看完不用翻正文就能拿去汇报。
- **坑 2**:每章一句话结论写成套话("总的来说……很重要") → 必须包含对比或指向动作。
- **坑 3**:KPI 目标写"提升"或"优化" → 必须是具体百分比或数值,即使是假设值。
- **坑 4**:附录 I 简单写"参考了行业报告" → 每个数据源要写清:报告名、发布方、发布日期、URL(如有)。
- **坑 5**:装完报告没读一遍 → 至少通读 2 遍,一次找逻辑漏洞,一次找错别字和格式。
- **坑 6**:内容还没定就先渲染 HTML → 先用 .md 跑完内容、过完自检,内容锁定后再渲染 HTML,否则改内容 HTML 也得返工。
- **坑 7**:HTML 用了外网 CDN 的图表库/字体 → 换个没网的会议室就打不开。**必须自包含单文件**,离线可开。
- **坑 8**:用 lieflat 出图时选了 Glance 或交互大图 → 它们依赖 ECharts/Chart.js 的 **CDN**,直接违反坑 7。**只用 Lupi Editorial / Lupi Basics 的纯 SVG 图型**,选型时明说"要离线可用"。
- **坑 9**:图放进 Tab 后不显示 → Tab 面板 `display:none` 时 SVG 没尺寸、观察器不触发。改成**切到该 Tab 才渲染**(dataset 去重)+ `beforeprint` 兜底,别指望页面一加载就画好。

## 归档

报告完成后:

1. 内容底稿存到品类目录根下:`{品类目录}/{品类}行业市场分析.md`
2. 渲染成最终交付:`{品类目录}/{品类}行业市场分析.html`(4 Tab 汇报页,规范见「HTML 汇报页输出规范」),过一遍 HTML 汇报页自检
3. 在工作流的 `README.md` 版本历史里追加一行:"YYYY-MM 完成 {品类}中档分析,反馈:XXX"
4. 把踩到的坑更新回工作流对应阶段的"常见坑"节
报告完成后:
