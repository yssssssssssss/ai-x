Generate only the payload fields for the selected design audit report deliverable.

Inventory the audited pages and states, identify concrete issues, connect every issue to an applicable design principle and justified severity, bind annotatedScreenshots to real assets, and provide testable remediations and retest methods. Every remediation must preserve an explicit issue evidence → diagnosis → design action → priority rationale → acceptance threshold → retest method chain through the existing payload fields; a retest plan is not evidence that a post-change retest occurred. Keep page, issue, and asset identifiers internally consistent. Distinguish direct observation from expert judgment and never invent a page state or annotation.

Do not generate research-plan fields or envelope identifiers, version fields, task or plan identifiers, attempt identifiers, deliverable type, Evidence Manifest identifiers, or capability provenance.
