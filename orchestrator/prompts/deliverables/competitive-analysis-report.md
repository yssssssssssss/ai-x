Generate only the payload fields for the selected competitive analysis report deliverable.

Build evidence-backed competitorSamples, a dimensionMatrix that compares every sampled competitor, explicit differences, audience impacts rooted in those differences, and prioritized actionRecommendations. Every recommendation must form a traceable chain through the existing payload fields: evidence → observed difference or phenomenon → bounded problem/opportunity attribution → insight → strategy → concrete action → priority rationale → metric → validation method. Do not invent a parallel payload field for this chain. If the finalized requirement defines a scoring system, copy every named dimension and confirmed weight into dimensionMatrix, include each sample's numeric score on the requested scale, and explain that score in value; do not invent a score when the supplied analysis cannot support it. If the requirement requests a management summary, scoring method, roadmap, instrumentation, or user-test script, populate managementSummary, scoringMethod, roadmap, instrumentationPlan, and userTestScript from the supplied analysis outputs. Preserve the requested P0/P1/P2 levels, target metrics, validation methods, task steps, probes, success criteria, and measurement definitions rather than collapsing them into generic advice. Use visualEvidence for a selected displayable browser-captured image, binding the real Asset id to both its screenshot Evidence and same-URL public_source Evidence. User-upload visual presentation is system-generated from verified provenance and explicit pairing metadata; return an empty screenshotComparisons array and do not choose, omit, pair, or label uploaded Asset ids. Without a displayable browser visual, return an empty visualEvidence array. Keep sample, difference, evidence, and asset identifiers internally consistent. Separate observed facts from interpretation, disclose evidence gaps, and never invent a competitor claim or screenshot.

Do not generate research-plan fields or envelope identifiers, version fields, task or plan identifiers, attempt identifiers, deliverable type, Evidence Manifest identifiers, or capability provenance.
