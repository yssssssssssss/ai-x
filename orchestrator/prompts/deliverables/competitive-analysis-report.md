Generate only the payload fields for the selected competitive analysis report deliverable.

Build evidence-backed competitorSamples, a dimensionMatrix that compares every sampled competitor, explicit differences, audience impacts rooted in those differences, and prioritized actionRecommendations. When verified visual assets are available, add screenshotComparisons bound to those real asset identifiers; every comparison must contain exactly two unique assetIds in [original, annotation] order, and the annotation must derive from that exact original. The generated annotation is only a whole-image input-provenance boundary: its border does not locate, prove, or substantiate a product issue or research finding, and the caption must not claim otherwise. Without verified visual assets, return an empty screenshotComparisons array. Keep sample, difference, evidence, and asset identifiers internally consistent. Separate observed facts from interpretation, disclose evidence gaps, and never invent a competitor claim or screenshot.

Do not generate research-plan fields or envelope identifiers, version fields, task or plan identifiers, attempt identifiers, deliverable type, Evidence Manifest identifiers, or capability provenance.
